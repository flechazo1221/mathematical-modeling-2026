# 控制器候选独立审计报告

审计时间：2026-09-12 19:35（Asia/Shanghai）  
审计范围：`controller-phase-change-attempt-20260912`、`controller-temp-field-completion-20260912`，以及正式 H2/H3、COMPUTE 和设计约束。  
写入边界：仅本目录；未修改正式 COMPUTE、EVIDENCE、FIGURE、PAPER、decisions 或 workflow 文件。

## 结论

**正式改进模型：不可用（BLOCKED）。论文用途：只能作为明确标注的等效潜热敏感性/上界诊断。**

它不能被写成“已验证的液—汽—结合水潜热模型”，不能宣称识别出 `L_v`、相变速率 `Gamma` 或相边界分配，也不能替代正式批准的 M1–M4 主路线。

## 关键证据

1. **运行完成但物理闭合失败。** 候选目录的 `原始结果.csv` 有 Q2/Q3/Q4 共 69/69 条成功求解记录，线性求解器和有限性检查通过。`water-audit.csv` 的最大总水分方程残差约 `1.13e-13 kg/s`，但这只是总 `C` 方程的库存—边界通量残差；候选没有独立的液水、汽水、结合水库存，也没有相分辨边界通量。因此 `PC-V06 mass-energy-closure=FAIL`，不能把总水分守恒当作相变守恒。

2. **参数不可分辨。** `identifiability.csv` 对 Q2/Q3/Q4 均给出两参数 Jacobian 秩 `1/2`、条件数 `inf`。当前源项只通过 `L_v × proxy_scale` 可见；同乘积交换实验的温度和事件结果一致，不能分别定出 `L_v` 与通量缩放。

3. **离散稳定性不足以支撑升级。** 独立空间梯子的 H−M 事件时间变化为 `[3480, 2040] s`，独立时间梯子的 H−M 变化为 `[420, 420] s`，均超过预注册的 `60 s` 报告容差；故 `PC-V03`、`PC-V04` 为 `INCONCLUSIVE`。候选边界热账本本身的相对残差很小（固定域最大约 `1.72e-7`，移动域约 `6.28e-8`），但这只说明等效边界热通量的数值账本可计算，不能修复缺失的相质量闭合。

4. **内部温度场候选没有形成可靠改进证据。** `controller-temp-field-completion-20260912/energy-audit-summary.csv` 中，内部温度候选的固定域最大相对能量残差约为：Q2 `1.90`、Q3 `1.85`、Q4 `1.97–1.99`；Q2/Q3 标记 `FAIL`，Q4 为 `INCONCLUSIVE`，且细化没有显示稳定的单调下降。该目录自身也记录没有内部温度观测，V11 为 `BLOCKED`。

5. **结果与文档存在边界不一致。** 实际结果包含 `space=24`、`time=24` 条记录，但 `候选方案对比.md` 的前置表仍写着“本轮未重复独立时间梯子/未实施”。这不改变拒绝正式升级的结论，但论文引用前必须以最终冻结的结果、脚本和报告重新对齐。

6. **代码—结果时间顺序要求重跑确认。** 当前候选 `run.log` 和主要结果在约 19:29:37 写入，`run_phase_change_attempt.py` 在约 19:33:40 更新，之后才在约 19:34:31 完成派生输出整理。最终 manifest/handoff 的内部哈希已可复核，但原始 69 次运行是否由当前脚本版本产生不能仅凭现有文件确认；若要作为可复现实验证据，必须冻结脚本后重新运行并重新生成全部派生文件。

## 关于 M0

在审计范围内**没有找到名为 M0 的正式模型、M0 决策记录或独立的 M0→M1→M2 潜热模型层级**。正式批准文件采用 M1–M4；候选目录的 `no_latent` 只是无潜热配对基线标签，不是一个已登记的 M0。`model-baseline` 和 `model-internal-temperature` 也是温度场候选实现标签，不能解释成 M0/M1/M2 的相模型层级。

## 允许与禁止的论文表述

- 允许：说明“把现有总水分 Robin 边界损失转为等效表面潜热通量”的条件敏感性，并报告低/名义/高场景及其不确定性。
- 禁止：写成已建立液—汽—结合水模型、已识别材料潜热、已验证相变动力学，或把 Q2/Q3/Q4 的条件差值写成实验精度提升。

## 重新打开正式升级所需的最低证据

需要独立的相状态/库存与边界通量闭合（至少液、汽、结合水及 `Gamma`），与移动域能量方程一致的潜热项；材料专属参数或可追溯测量；内部温度和相分辨观测；冻结脚本后的独立空间/时间细化、留出工况、守恒和 profile-identifiability。完成后才可重新提交 H2 选择，不能在当前 BLOCKED handoff 上覆盖为 PASS。

## 审计产物与依据

详细机器可读结论见同目录 `handoff.json`。主要依据为：

- `03-prototype/controller-phase-change-attempt-20260912/handoff.json`
- `03-prototype/controller-phase-change-attempt-20260912/原始结果.csv`
- `03-prototype/controller-phase-change-attempt-20260912/heat-audit.csv`
- `03-prototype/controller-phase-change-attempt-20260912/water-audit.csv`
- `03-prototype/controller-phase-change-attempt-20260912/identifiability.csv`
- `03-prototype/controller-temp-field-completion-20260912/handoff.json`
- `03-prototype/controller-temp-field-completion-20260912/energy-audit-summary.csv`
- `decisions/H2-model.json`、`decisions/H3-claims.json`


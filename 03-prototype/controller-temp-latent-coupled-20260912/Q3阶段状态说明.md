# Q3 阶段状态说明：温度场—表面等效潜热候选

状态：**Q3 结果已保留；本目录不再启动新的 Q4 算例。** Q4 已拆交由独立任务运行。

## 执行边界

- `原始结果.csv`、`候选配对比较.csv`、`space-time-refinement.csv`、`heat-audit.csv`、`water-audit.csv` 等汇总文件来自此前已完成的 69 作业梯度；其中包含历史 Q4 行，不能作为本次 Q3-only 交接的 Q4 任务输入。
- 为补齐独立 `failures.csv` 并排除 `__pycache__/*.pyc`，曾启动一次元数据一致性重跑；按总控拆分要求已在新的 Q4 算例前停止。当前 `run.log` 是该次中断重跑的部分日志，不能与旧汇总表拼接宣称为新的完整梯度。
- 没有删除任何已有结果或失败记录；`原始结果.csv` 是逐作业权威表，`failures.csv` 是本目录的独立失败登记（本次已完成 Q3 子集无失败）。

## 已完成 Q3 证据

从既有完整梯度的 `原始结果.csv` 筛选 Q3：23 行，23 行数值 PASS，0 行 FAIL。最大线性求解相对残差为 `4.99997645405861e-11`；最大物理热量相对残差为 `0.0090856507561496`；最大水量账残差为 `7.18576063994496e-14 kg/s`。这些是数值审计结果，不是内部温度实测验证。

名义等效潜热候选的独立梯度显示：

- 空间梯度（M 网格与 H 网格）事件时间差为 `2040 s`；
- 时间梯度（M 网格，M/H 时间步）事件时间差为 `420 s`；
- 预登记的 `60 s` 事件时间误差标准未满足，不能用最终含水率或温度差替代该标准；
- `L_v` 与 `proxy_scale` 只通过乘积进入，`identifiability.csv` 给出 Jacobian 秩 1、条件数 `inf`，因此不能分别识别。

## 物理解释边界

本候选仅是“总含水率单场 + 由现有总水分 Robin 边界通量构造的表面等效潜热”。水量守恒审计的是总含水率库存与总边界通量；没有独立液态、气态、结合水库存，也没有相分辨边界通量。因此 `latent_mass_closure` 保持 FAIL，不能写成三类水已识别或潜热已由数据验证。

## 当前交接

H2 仍须人工复审，不能设置 `TEAM_APPROVED`，不能推进正式 COMPUTE。若继续 Q3 工作，应使用本说明和既有 Q3 行；Q4 结果、日志与收敛审计由独立 Q4 任务另行生成。

# FIG-Q2-GRID-CONV 美化版视觉审计

- 状态：重绘完成，待团队确认；未写入 `approval-register.json`。
- 数据：沿用冻结快照 `data-snapshots/FIG-Q2-GRID-CONV.csv`，哈希与登记值一致：`d8a9ca7c399aaa16357bf8e2b059484f92c733d950fd76b69ea6acc9ea1a7f0a`。
- 数值：最细相邻变化 `4.0011396525e-05`，验收线 `5e-05`，保留“窄裕量 PASS”。
- 结构：主图展示 `final_max_C` 随网格轨迹；下方单独展示相邻网格变化与验收线，避免双 y 轴单位混淆。
- 轴说明：横轴明确为“空间网格（径向网格数 nr × 轴向网格数 nz）”，纵轴明确为“3 h 最大干基含水率 C（kg/kg）”；下方轴明确为相邻网格变化及其单位。
- 图例：新增“ M3 网格收敛轨迹”和“最细网格结果”图例，文字使用黑体并设为黑色。
- 文件：SVG、PDF、600 DPI PNG，以及彩色/灰度预览均已生成。
- 视觉检查：中文无方框；科学计数法上标完整；坐标轴、标注和图注未裁切；彩色与灰度下曲线、两组相邻变化和验收线均可区分。
- PDF 字体：`pdffonts` 检查显示 SimSun、SimHei、ArialMT、DejaVuSans 均已嵌入 TrueType 子集。
- 解释边界：该图只支持数值离散化收敛诊断，不支持现实药材准确性结论。

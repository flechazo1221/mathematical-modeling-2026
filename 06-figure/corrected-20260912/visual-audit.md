# FIG-DATA-EXTENSION-WIDE 视觉与程序审计

## 视觉检查

- 彩色预览与灰度预览均已查看：观测段为高对比蓝色实线加圆 marker，冻结延拓段为高对比橙色实线加方 marker。
- 坐标轴、网格、标题、图例、刻度和轴标签统一为黑色；中文字体使用宋体；图例未遮挡数据。
- 图例两行已使用相同的行中心、marker 中心和文字锚点，文字与线段水平对齐，且上下边距一致。
- 已删除“4 h 延拓起点”竖线文字和图内额外备注；未发现裁切、缺字或遮挡。

## 数据检查

- 原始附件 1 的 241 个温度采样点全部保留，源数据 SHA-256 与冻结快照逐点核对一致，最大绝对差为 0。
- 坐标范围保持为时间 `[0, 10] h`、温度 `[25, 55] °C`；4–8 h 延拓值保持既有 `50.165 °C` 口径。

## 程序检查

- 输出包含 SVG、PDF、600 DPI PNG，以及彩色/灰度预览。
- `check_figure.py --min-dpi 300 --strict` 对上述 5 个输出无 FAIL；PDF 字体检查通过；SVG 不含 base64 位图。
- 具体尺寸、DPI、文件哈希和数据核验结果见 `qa/FIG-DATA-EXTENSION-WIDE-qa.json`。

## 工作流边界

- 本轮只修订 `FIG-DATA-EXTENSION-WIDE`，不修改同目录其他图片，不重算模型，不推进 PAPER。
- H3 官方 22 图清单不被擅自增删；本图作为现有校正版目标的呈现修订留痕。

# 已放弃图件：FIG-Q2-V02-MARGIN

团队决定：V02 窄裕量用正文文字表述更清楚，不再制作或保留为正式论文图。

- 状态：`OMITTED_BY_TEAM`
- 原图件：保留在本目录，仅作可恢复留痕，不属于当前 `06-figure/figure-manifest.json` 的活动交付。
- 正文应保留事实：V02 的最大登记场变化为 `4.00114e-5`，验收阈值为 `5e-5`，属于窄裕量；不得写成宽裕或零误差。
- 上游计算结果、V02 数据及 H3 原始决议不修改。

## 已放弃图件：FIG-Q3-COMBINED-BOUNDARY

团队决定：Q3 组合边界不在论文中插图，改用正文文字说明；原图件和数据快照保留，仅作可恢复留痕，不删除上游证据。

- 状态：`OMITTED_BY_TEAM`
- 论文处理：不插入 `FIG-Q3-COMBINED-BOUNDARY`，不保留该图题或图号引用。
- 证据边界：保留 Q3 的 `pref-low`、`exponent-high` 和 `combined-low` 在 72 h 内未达到全域阈值的事实；`259200 s` 是计算上限，不是达标时刻。
- 文字替代表述：

  > 在预注册的有界扰动下，经验式系数取低值、指数取高值以及组合低情景均未在 72 h 内达到全域含水率阈值。对组合低情景，参数缩放为 `(0.8, 0.8, 0.8, 1.2)` 时，截至 `259200 s`（72 h）全域最大含水率仍为 `0.182493 kg/kg`，高于 `0.15 kg/kg` 的判据；相对地，组合高情景在 `111780 s`（约 `31.05 h`）达到阈值。因此，72 h 应理解为本次压力测试的计算上限，而非达标时刻；该结果只说明 Q3 时间结论依赖冻结的边界延拓与经验参数，不能解释为概率区间或无条件外推。

- 上游计算结果、Q3 主张类别及其他正式图表不修改。

## 已放弃图件：FIG-VAL-BALANCE-RESIDUAL

团队决定：守恒与线性残差不作为论文独立图件，改由验证表和正文简短说明承载；原始快照、合同和验证记录保留，不删除上游证据。

- 状态：`OMITTED_BY_TEAM`
- 论文处理：不插入该图，不保留其图题或图号引用；活动图表清单由 16 张主文图调整为 15 张。
- 证据边界：批准路线最大归一化平衡误差为 `9.6451610e-8`，最大线性残差为 `5.0000647e-11`；这些数值只说明数值实现质量，不代表现实药材准确性。
- 可恢复留痕：渲染图件移入本目录的 `figures/`、`previews/` 和 `captioned/` 子目录；`06-figure/data-snapshots/`、`06-figure/contracts/` 及验证表保持原位。

## 已放弃图件：FIG-Q4-JACOBIAN-ABLATION

团队决定：Jacobian 消融的两个守恒失衡值通过正文和验证表说明即可，不再作为论文独立图件；原图件移入本目录，仅作可恢复留痕，不删除上游证据。

- 状态：`OMITTED_BY_TEAM`
- 论文处理：不插入该图，不保留其图题或图号引用。
- 证据边界：V10 失败消融必须保留。批准路线最大归一化守恒失衡为 `9.6451610e-8`，删除 Jacobian 后为 `0.5936318973`；该结果只说明移动域数值守恒离散的必要性，不代表真实药材准确性。
- 可恢复留痕：原图件移入本目录的 `figures/` 和 `previews/` 子目录；`06-figure/data-snapshots/`、`06-figure/contracts/`、`TAB-VAL-V01-V12` 及 H3 原始决议保持原位。

## 已放弃图件：FIG-Q4-IMPLEMENTATION-AGREEMENT

团队决定：Q4 reference 与 moving-FV 双实现一致性不作为论文独立图件，改由正文和证据表保留数值与分析；原图件移入本目录，仅作可恢复留痕，不删除上游证据。

- 状态：`OMITTED_BY_TEAM`
- 论文处理：不插入该图，不保留其图题或图号引用。
- 文字替代表述：

  > 收缩域参考实现与 moving-FV 实现的最细网格插值阈值时刻均为 `183789.3317 s`，数值差 `5.82e-10 s`，按 `60 s` 报告均为 `183840 s`；相对于约 `1.84×10^5 s` 的事件时刻，该差值仅为 `10^-15` 量级。该结果只说明两种数值实现的一致性，不构成外部实验验证。

- 上游计算结果、Q4-OBS-01 主张、数据快照和合同保持不变。

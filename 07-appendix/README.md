# 附录撰写阶段占位

状态：RESERVED_SKILL_PENDING

逻辑顺序：

FIGURE → 可选 C1 → APPENDIX → PAPER

本目录只用于预留未来的“附录撰写”独立阶段。当前不得在此生成附录正文、代码清单、表格、声明或其他实质内容。

在用户提供并批准具体 Skill 前：

- APPENDIX 不得启动；
- 不得创建伪造的 PASS handoff；
- PAPER 不得绕过本阶段；
- 若工作流提前到达此处，控制器必须停止并报告 BLOCKED。

未来 Skill 接入时必须补齐：允许读取的输入、唯一写入边界、必需输出、handoff schema、完成检查、监督基线以及 PAPER 对 APPENDIX PASS 交接的依赖。


# 论文证据大纲（H3 前中立稿）

本大纲只组织证据，不选择最终主张、图表或叙事。数值权威源为 `04-compute` 的正式结果；补充扩展不得替代 M1–M4。

## 1. 问题与统一口径

- 交代状态变量、初始条件、0–4 h 边界与 4 h 后稳健统计延拓。
- 明示 Q3/Q4 停止判据为全域 `max C < 0.15 kg/kg`，计算比较使用 `0.15-1e-6`，报告时间向上取至 60 s。
- 说明 M1 为一维基线、M2 为二维主干、M3 为状态相关主路线、M4 为题面可识别耦合消融。

## 2. 数学与数值方法证据

- 轴对称守恒有限体积、后向欧拉、系数 Picard、预条件 CG；精确合同见 `02-design/contracts/`，实现见 `04-compute/src/formal_compute.py`。
- Q4 使用随动收缩域与干固体连续性；必须保留 Jacobian/几何修正。
- 复现命令以 `04-compute/复现清单.json#/commands` 为定位入口。

## 3. Q1：基线与二维效应

- M1/M2 1800 s 平均含水率：2.2935805570 与 2.2750348485 kg/kg；差 0.0185457085。
- M2 样本中 1800 s 中心/表面含水率为 2.5499915369/1.5101482988 kg/kg。
- 正文必须把模型间差异与各模型网格误差分开，不使用“精度提升”。

## 4. Q2：状态相关路线与消融

- 3 h 最大含水率：M3 1.7278463920、M2 常物性 2.1599687487、M4 消融 1.7645199232 kg/kg。
- V02：最细相邻网格变化 4.00114e-5，对 5e-5 目标仅窄裕量 PASS；必须使用最细登记值。
- 明示没有内部观测，三模型差异是条件仿真差异。

## 5. Q3：全域阈值事件

- 最细空间网格插值事件时刻 206818.7114 s，正式 60 s 报告值 206820 s。
- M4 消融报告 212640 s，只能用于机制消融比较。
- V06：时间步细化最大变化登记为 48 s，对 60 s 目标裕量有限。
- 边界失败必须出现：prefactor-low、exponent-high、combined-low 在 72 h 内未越阈值。

## 6. Q4：移动边界、守恒与达标时间

- 最细空间网格插值时刻 183789.3317 s，正式报告 183840 s。
- 参考实现与 moving-FV 实现差 5.82e-10 s，相对于约 1.84×10^5 s 的事件时刻仅为 10^-15 量级；这验证实现一致性，不验证现实准确性。
- 批准路线最大归一化平衡误差 9.6451610e-8；Jacobian 消融失衡 0.5936318973，故消融路线拒绝。
- V12：干固体相对误差 4.441e-16，局部连续性残差 2.384e-13，几何误差 0。
- 组合低情景在 72 h 内未达标，必须与成功情景同时报告。

## 7. 收敛、守恒与稳健性总表

- 完整列示 V01–V12；不能只写“全部通过”。
- 对 V02、V06 单独标注有限裕量。
- 对敏感性使用“有界压力测试”，不用“置信区间”“概率”或“因果”。

## 8. 补充实验（可选进入论文）

- CFD：仅缩减机制探针，状态 `INCONCLUSIVE_MECHANISM_PROBE`。
- PINN：仅规范化稀疏反演，状态 `SUPPORTED_CANONICAL_ONLY`。
- 代理：仅冻结域内插值筛查，状态 `RETAIN_INTERPOLATION_ONLY`。
- 纯 ML：阈值时间 RMSE 2808/4469.7 s，作为负对照。
- 所有标签均为合成 PDE 数据，不得称为实验数据或经验验证。

## 9. 限制与适用范围

- 无内部实验观测，不能宣称拟合优度、经验准确率或真实机制识别。
- 题设经验式只支持本题冻结工况下的条件仿真。
- 敏感性范围不穷尽结构不确定性，且存在 72 h 内不达标边界案例。
- 诊断图只用于模型检查，正式图须按 `正式图表需求.json` 重新制作并保持数据可追溯。

## 10. 正式图表证据编排

- `正式图表需求.json` 已把原先复合需求拆为 36 项独立、可执行、非重复的图表需求：29 图、7 表；首选位置为主文 19 项、附录 17 项。
- Q1 分开表达水分场、温度场、端面效应、网格结构与空间收敛；Q2 分开表达剖面、模型消融、V02 收敛轨迹及窄裕量。
- Q3 分开表达全程阈值轨迹、局部插值夹逼、空间收敛、时间收敛、单因素与组合失败边界。
- Q4 分开表达半径收缩、阈值轨迹、插值夹逼、空间收敛、Jacobian 消融、干固体连续性及敏感性；双实现一致性保留为文字量化分析。
- 跨问题图表单列守恒/线性残差、V01–V12、主张限制和适用边界；补充路线仅保留为附录对照候选。
- H3 三个叙事选项均在 `H3-选择包.md` 中给出完整的主文图、主文表、附录图、附录表，以及最小集和扩展集；团队可选择但不得删除强制失败证据。

## 11. H3 待团队决定

- 选择哪些候选事实进入核心结论与摘要。
- 选择正式图表组合及是否保留补充实验章节。
- 选择叙事重心，同时接受对应限制与强制披露。
- 逐项确认主文/附录分配、最小集/扩展集和补充实验 S0–S3；只有真实队员可批准。

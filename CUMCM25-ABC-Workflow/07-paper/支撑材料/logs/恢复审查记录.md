# COMPUTE 恢复审查记录

时间：2026-09-07（Asia/Shanghai）

## 审查边界

- 权威决议：`decisions/H2-model.json`（`TEAM_APPROVED`）。
- 对照实现：`03-prototype/run_prototypes.py` 与 `03-prototype/预注册规则.json`。
- 写入边界：仅 `04-compute/`。

## 已知旧偏离（保留为失败历史）

监督停止前的初版 COMPUTE 曾把冻结的 F 改成 8 倍零填充并加入亚栅格峰值插值，把 P 改成 601 点频率网格并加入亚栅格插值。上述做法改变了 H2 所依据的原型算法，属于结构/参数偏离。该历史不删除、不掩盖；旧任务在停止前仅自述恢复并运行 smoke，恢复后尚无独立 P1 结论，也未运行 full。

## 当前逐项复核

| 检查项 | 冻结/原型规则 | 审查时现状 | 本次处理 |
|---|---|---|---|
| F 窗函数 | Hann | `np.hanning(n)` | 保持 |
| F FFT 长度 | n 点 | 主估计 `zero_pad_factor=1` | 保持 |
| F 峰值 | 离散 FFT 峰，不作亚栅格插值 | 无亚栅格插值 | 保持 |
| F 低频排除 | 原型将 bins 0、1 置零后从 bin 2 起选峰 | 审查时 `freq >= 1/span` 会纳入 bin 1 | 已修正为仅从 bin 2 起选峰 |
| P 搜索网格 | 401 点离散频率网格 | `np.linspace(lower, upper, 401)` | 保持 |
| P 峰值 | 离散网格最小值，不作亚栅格插值 | 无亚栅格插值 | 保持 |
| 8 倍零填充 | 不得用于主 F；仅可作标记清楚的诊断 | 仅在 `fft_pressure_test` 的诊断变体中调用 | 保持；结果不得覆盖主 F |
| 窗口 | W1/W2/W3 全报 | 600–1600、1600–2800、2800–3800 | 保持 |
| 模型角色 | B 基准、F 主、P 补充、M 关闭 | 与 H2 一致 | 保持 |

## 审查结论与停止条件

当前代码在上述单点修正后满足 F=n 点 Hann FFT 离散峰、主结果无零填充和无亚栅格插值，P=401 点离散网格且无亚栅格插值。下一步必须由独立 P1 执行最小命令复核；只有明确 `PASS` 才允许 full。若 P1 `FAIL`，立即停止且不进行第二轮自行修复；若冻结 F 结构不可行，则返回 `FAIL + REQUEST_REOPEN_H2`。

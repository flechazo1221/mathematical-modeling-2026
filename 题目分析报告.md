# 2026 年高教社杯全国大学生数学建模竞赛 A/B/C 题分析报告

> 阶段：00 分析赛题（只完成题目理解、数据审计和候选模型设计，不替队伍选择最终题目）。
>
> 本报告是 `math-modeling` Skill 生成的建模草稿，不能未经队伍人工核对直接作为参赛论文或支撑材料。

## 1. 输入清单与可复核性

题面来自 `CUMCM2026Problems.zip` 及用户单独提供的三份 PDF。压缩包 SHA-256：

`A54C0E6B552D31E2DBD41ABA4A07769943433CB9317927514C2D39EC6442E241`

压缩包内确认包含：

| 题目 | 题面 | 配套输入 | 模板/协议 |
|---|---|---|---|
| A | `A题/A题.pdf`，4 页 | `附件1.xlsx`、`附件2.xlsx` | `附件3/result1.xlsx` 至 `result4.xlsx` |
| B | `B题/B题.pdf`，4 页 | 无静态数据表；案例由模拟器随机生成 | `附件1.docx`、`附件2.docx` 为操作与 HTTP+JSON 协议 |
| C | `C题/C题.pdf`，3 页 | `附件1.xlsx` 至 `附件4.xlsx` | `附件5/result1.xlsx`、`result2.xlsx`、`result3.xlsx`、`result4-2.xlsx`、`result4-3.xlsx` |

本轮实际读取了上述题面、A/C 数据表、A/C 模板和 B 两份协议文档；原始输入均保持只读。A/B/C 题面 SHA-256 分别为：

- A：`052D8014BFF5727C019B72E44FDFFAF5C145CE04050DD938BAAF3527DB331736`
- B：`81C992A9BEE5376308C4768B58719B177F17D6EE1F9861577A851924C5A838CA`
- C：`2C098F6AE9DD47AE965AEBDEC3B9FACF3DE6C173F999783C1012C08FC5FB9D2D`

题面明确属于 2026 年高教社杯全国大学生数学建模竞赛。具体论文格式硬约束应以压缩包中的 `format2026.doc` 和官方发布页面最终核验；本阶段不把往届经验当作官方规则。

## 2. 三题总体判读

| 题目 | 主类型 | 核心难点 | 数据/依赖风险 | 主要证据 |
|---|---|---|---|---|
| A | 机理建模、偏微分方程、移动边界 | 热传导与水分扩散耦合、边界换热/传质、半径收缩 | 边界环境的分段定义、变量系数稳定性、长时间计算 | PDE 残差、边界条件、网格/时间步收敛、阈值时刻 |
| B | 几何定位、在线黑箱搜索与路径决策 | 不能读取案例真值；要在时间窗内发现 10–16 个源并清除 | 模拟器与网络、正式测试机会、协议幂等性、算法运行时间 | 几何上界、演练统计、正式日志、清除率和平均时间 |
| C | 线性/混合整数优化、滚动时域控制 | 计划购电、储能状态、预测误差、紧急/调整/违约费用统一 | 附件时间对齐、10 分钟与整点预测的映射、循环末状态 | 可行性、费用分解、SOC 边界、预测回测和策略对照 |

目前不建议仅凭“题型熟悉度”决定选题。若队伍没有稳定 PDE 数值基础，A 的实现风险最高；若没有模拟器可运行环境和足够演练时间，B 的实验性风险最高；C 的数据和模板最完整，最适合先做可行性原型，但需要严谨处理预测—实际对齐与多层费用规则。

## 3. A 题：药材的烘干问题

### 3.1 题目要求重述

- 问题 1：在预热平衡阶段，给定圆柱药材初始温度/水分浓度、烘房温度/水分浓度时间序列和常参数，求内部温度场与水分浓度场，并输出 0–1800 s、0–2 cm 的完整网格。
- 问题 2：使用附录 3 的 (C,T) 相关物性，建立预热平衡与恒温干燥相接的全流程模型，输出 3 h 结果。
- 问题 3：求内部所有位置的水分浓度均低于 (0.15 mathrm{kg/kg}) 的最早时间。
- 问题 4：引入附件 2 的半径变化和附录 4 物性，求移动边界下的烘干时长与浓度场。

### 3.2 数据审计

| 文件 | 实际结构 | 关键范围/质量 |
|---|---|---|
| A/附件1.xlsx | 1 表，241 条数据，3 列 | 时间 0–14400 s；烘房温度 28–50.246 °C；边界水分浓度约 0.01963–0.05025 kg/kg；无缺失数值 |
| A/附件2.xlsx | 1 表，145 条数据，2 列 | 时间 0–259200 s；半径 2.000–1.198 cm；无缺失数值 |
| A/附件3 模板 | 2 个温度/水分浓度表或 1 个浓度表 | 空模板，需保留工作表与首行距离字段；不能覆盖原模板 |

需要在编程阶段再次核对中文表头编码、时间是否等间隔、附件 1 的边界量到底对应题目中的空气水分浓度还是其他湿度定义，以及 1800 s 后恒温干燥边界的精确定义。

### 3.3 模型合同与候选路线

采用圆柱轴向均匀、径向轴对称的一维模型。令 (rin[0,R(t)]) 为距中心的半径坐标，(T(r,t)) 为温度，(C(r,t)) 为干基水分浓度。

基础耦合模型为

\[
 \rho(C)c_p(C)\frac{\partial T}{\partial t}
 =\frac1r\frac{\partial}{\partial r}\left(rk(C)\frac{\partial T}{\partial r}\right),
 \qquad
 \frac{\partial C}{\partial t}
 =\frac1r\frac{\partial}{\partial r}\left(rD(C,T)\frac{\partial C}{\partial r}\right).
\]

中心对称条件为 (partial_rT(0,t)=partial_rC(0,t)=0)。表面使用对流换热/传质 Robin 条件，例如

\[
 -k\partial_rT|_{R}=h_T(T_s-T_a(t)),
 \qquad
 -D\partial_rC|_{R}=h_m(C_s-C_a(t)),
\]

其中 (T_a(t),C_a(t)) 来自附件 1；(h_T,h_m) 以及物性参数按题目附录使用。初值为 (T(r,0)=28^circmathrm C, C(r,0)=2.55 mathrm{kg/kg})。温度进入指数公式前必须转换为 K；空间离散必须统一为 m，输出再转换为 cm。

| 子问题 | 主模型（建议） | 可选对照模型 | 输入/输出 | 验证 |
|---|---|---|---|---|
| A1 | 常系数/给定边界的轴对称热—质扩散有限体积或隐式有限差分 | 固定步长 Crank–Nicolson | 输入附件 1、初值、附录 2；输出 (T,C) 时空网格 | 中心对称通量、表面通量、正值性、网格/步长收敛 |
| A2 | 变量物性、分段边界的非线性隐式有限体积 | 变量物性显式/半隐式仅作小步长对照 | 输入附录 3、A1 末状态；输出 3 h 场 | 能量/水分守恒、非负性、不同网格结果差异 |
| A3 | 在 A2 上搜索 (g(t)=max_r C(r,t)-0.15) 的首次过零时刻 | 时间插值与二分细化 | 输出烘干时长和阈值时刻场 | 检查全空间最大值而不是只看表面；时间步收敛 |
| A4 | 参考坐标 (x=r/R(t)in[0,1]) 的移动边界 PDE；(R(t)) 由附件 2 插值 | ALE/直接重网格法对照 | 输入半径序列、附录 4；输出到“药材表面” | 半径拟合误差、坐标变换正确性、固定半径退化测试 |

### 3.4 关键假设、风险与停止条件

关键假设应控制在少数几条：

1. 药材可视为轴向均匀、圆柱且径向对称；若附件或实验说明显示轴向差异，必须停止使用一维模型。
2. 热量与水分传输可由题目给定的局部物性和扩散/对流边界描述；题目未给出的内部对流、相变潜热不能擅自添加。
3. 半径随时间的变化可由附件 2 插值，并作为外部给定边界；若题意要求浓度反过来决定收缩，则需补充收缩定律，不能仅凭半径数据声称因果。

可放宽假设包括边界数据线性插值、空间均匀初值和时间步离散；分别用插值方案、初值扰动和网格/步长扫描复验。最大风险是题面没有明说 1800 s 后恒温阶段边界如何取，报告中应把“保持 1800 s 边界值”“采用题面指定恒温值”“分段拟合”列为待确认分支，未确认前不生成最终时长。

## 4. B 题：无线电干扰源的快速自动定位与清除

### 4.1 题目要求重述

- 问题 1：由若干检测点和示向度构造交会定位区域，计算其直径，并判断直径圆能否覆盖区域。
- 问题 2：已知一个检测点的示向度，为全向源选择第二检测点，使定位效果较好，并给出候选区域。
- 问题 3：全向源、数量未知但在 10–16 个，机器狗从原点出发，在时间限制内自动测量、定位、清除；演练后进行三次正式测试并提交日志。
- 问题 4：加入数量和方向未知的定向源，设计检测、判别、定位、清除策略并完成演练和正式测试。

### 4.2 协议与约束清单

输入是在线黑箱模拟器，不提供可直接读取的案例真值。目标区域是半径 1800 m 圆域；频道为 1–20 且每个源频道唯一；示向度误差全局在 ([-1^circ,1^circ])。机器狗速度 5 m/s，测量稳定读数 5 s，换频道 1 s，光学精定位 3 s、清除 2 s；距离不超过 20 m 才能清除，距离不超过 5 m 且在有效覆盖角内时测量返回 `near`。正式测试有 25 min 窗口、最多 20 min 程序运行时间和三次机会，需用唯一 `request_id` 保证幂等性。

### 4.3 模型合同与候选路线

**问题 1：几何可行域。** 对检测点 (S_i=(x_i,y_i))、测得方位 (	heta_i)，把误差带表示为两个边界射线围成的角扇形；定位区域是目标圆域与所有扇形的交集。可将边界线段和圆弧离散/解析求交，得到凸多边形近似 (P)。直径为

\[
 d(P)=\max_{p,q\in P}\|p-q\|_2,
\]

对凸多边形可由顶点对或旋转卡壳计算。半径为 (d(P)/2) 的圆并不对任意集合都成立：应进一步计算最小包围圆半径 (r_{min}(P))，只有 (r_{min}(P)le d(P)/2) 时才可覆盖；不能把“直径定义”误当作“必有同直径圆覆盖”。

**问题 2：第二检测点。** 以两条方向线的交会角、误差带面积/直径和可接收距离为指标，优先最大化交会角的正弦并避免两点共线或过近。候选区域应满足：位于目标圆域内、与可能源位置的距离不超有效接收半径、与第一测点形成足够大的基线夹角。可先用几何指标筛选，再用蒙特卡洛源位置与 (pm1^circ) 误差模拟计算期望定位直径。

**问题 3：自适应搜索。** 建议采用“低成本覆盖扫描 + 方向交会 + 局部清除”的状态机：按螺旋/分层网格访问检测点，批量按频道测量；收到 `direction` 后安排第二点或第三点交会，接近目标后用 `clear`；收到 `near` 时直接进入 20 m 内清除策略。每次行动以预期信息增益/单位时间为优先级，路径代价用直线距离除以 5 加测量、换频和清除时间。演练数据用于调节扫描间距、频道顺序、二次检测阈值和返程策略，正式测试只能使用协议允许的信息。

**问题 4：定向源判别。** `no_signal` 不能直接等同于“无源”，因为定向源可能不在有效覆盖角内。应在候选区域对同一频道进行多地点/多方向观测，维护“存在概率 + 定向覆盖扇区”状态；发现方向信号后继续用交会定位，连续反向无信号则降低该源假设而不是立即删除。清除判定仍只依赖距离，不因定向源角度而改变 20 m 规则。

| 子问题 | 主模型 | 对照/验证 | 主要输出 |
|---|---|---|---|
| B1 | 误差扇形交集 + 凸区域直径/最小包围圆 | 蒙特卡洛角度误差 | 定位区域、直径、覆盖判定 |
| B2 | 几何交会角优化 + 接收半径约束 | 随机源位置模拟 | 第二检测点候选区域、定位质量曲线 |
| B3 | 信息增益驱动的自适应覆盖搜索与最短路局部规划 | 固定扫描基线、演练多案例 | 清除个数、清除率、平均定位清除时间、程序耗时 |
| B4 | 隐变量方向的概率状态机/候选扇区维护 | 全向源基线和不同扫描间距消融 | 全向/定向兼容策略、正式日志指标 |

最大风险不是公式，而是无法在本环境直接完成模拟器正式测试。没有演练和三次正式日志，B3/B4 只能交付算法设计，不能声称有效性或给出正式测试数值。

## 5. C 题：微网与外部电网电力调控策略

### 5.1 题目要求重述

- 问题 1：电价和负载日重复、光伏有一天预测，在供电不低于负载且首末储能相同的条件下制定日计划。
- 问题 2：电价日内相同、负载和实际光伏变化；计划不足时紧急购电，费用为交易时刻电价的 5 倍，制定 2025 年 2 月 1 日至 12 月 31 日策略。
- 问题 3：每日 0/6/12/18 时更新未来 24 h 光伏预报，允许调整计划并承担偏差费用，比较是否需要其他预报时刻。
- 问题 4：电价实时波动，分别重算问题 2 和问题 3。

### 5.2 数据审计

| 文件 | 实际结构 | 审计结果 |
|---|---|---|
| C/附件1.xlsx | 144 个 10 分钟时段，电价、负载、预测光伏 | 电价约 0.3713–1.3952 元/kWh；负载约 3309.3934–5958.9696 kW；预测光伏 0–7612.316 kW；无缺失 |
| C/附件2.xlsx | 365 天 × 144 个 10 分钟时段，负载/实际光伏两表 | 日期 2025-01-01 至 2025-12-31，365 个唯一日期；日负载能量约 71490–141566 kWh，日光伏能量约 34361–75223 kWh |
| C/附件3.xlsx | 1460 行，即 365 天 × 4 个发布时刻；每行 24 个整点预测 | 发布时刻为 0:00、6:00、12:00、18:00；需严格将整点预测映射到 10 分钟控制时段 |
| C/附件4.xlsx | 365 天 × 144 个 10 分钟电价 | 无缺失；用于问题 4 的波动电价 |
| C/附件5 模板 | 与各问题对应的计划、调整、充放电、紧急购电工作表 | 必须保留工作表、日期行、时间段和单位，不覆盖原模板 |

### 5.3 统一优化模型

取 (Delta t=1/6) h，决策变量包括计划购电 (g_t^p)、调整购电 (g_t^a)、紧急购电 (g_t^e)、充电功率 (p_t^c)、放电功率 (p_t^d)、弃光 (w_t) 和储能量 (s_t)。功率平衡可写为

\[
 g_t^p+g_t^a+g_t^e+p_t^d+q_t^{pv}
 = L_t+p_t^c+w_t,
\]

\[
 s_{t+1}=s_t+\eta_c p_t^c\Delta t-\frac{p_t^d\Delta t}{\eta_d},
 \quad 1200\le s_t\le10800,
 \quad 0\le p_t^c,p_t^d\le5000.
\]

问题 1 加 (s_0=s_{144})；问题 2 以计划策略和实际光伏计算短缺，并令紧急购电覆盖不能满足负载的部分；问题 3 在预报更新时重新优化未来窗口，计划偏差、超计划调整和不足紧急量分别按题面费用计入；问题 4 将 (c_t) 替换为附件 4 的时变电价。若允许同时充放电会产生非物理解，应使用二元变量或证明正价格下的互斥性；不能仅依赖求解器“通常不会同时充放电”。

基本目标是最小化全天总成本：

\[
 \min\sum_t \Delta t(c_t^p g_t^p+5c_t g_t^e)
 +\sum_t \text{调整/违约费用}_t,
\]

其中调整费用必须按题面逐项拆分：计划高于调整部分按交易时刻电价的 50% 计违约费，调整高于计划部分的超出量按 1.5 倍电价计费。实现前要用几个手算时段验证符号方向，避免把“折价”与“罚价”写反。

### 5.4 分题实现和验证

| 子问题 | 方案 | 关键验证 |
|---|---|---|
| C1 | 日内 LP/MILP，计划光伏作为确定输入 | 每时段供电不低于负载；SOC 首末相等且不越界；费用分项求和 |
| C2 | 365 次滚动/逐日优化，实际光伏决定紧急量 | 对指定日期回放；紧急购电恰好覆盖短缺；日期、10 分钟时段和模板行数一致 |
| C3 | 0:00 初始计划 + 6/12/18 时滚动调整 | 比较仅 0:00、0/6、0/6/12、0/6/12/18 四种信息集；以总费用、紧急量、调整量和 SOC 稳定性评估 |
| C4 | 用附件 4 的逐时价格重算 C2/C3 | 同一负载/光伏输入下拆分价格波动贡献，检查计划/调整费用无遗漏 |

主要风险是附件 3 为整点预报而控制模板为 10 分钟；建议先把每个整点值作为该整点所在控制时段的前视信息，并对其他 10 分钟采用明确的零阶保持或线性插值，再用实际数据回测两种映射的费用差异。该选择必须写入模型假设和复现清单。

## 6. 预处理、编程依赖与交付计划

1. 先复制输入哈希与字段清单到内部复现记录；原始 PDF/XLSX/DOCX 不改写。
2. A 使用 Python 的 NumPy/SciPy 稀疏矩阵和隐式求解；先做短时、小网格收敛测试，再跑完整网格。输出模板需使用 `openpyxl`，保留工作表和格式。
3. B 使用 Python HTTP 客户端、几何计算和事件日志；每个请求独立 `request_id`，保存原始响应和演练/正式日志。若模拟器未提供，不进入“正式测试已完成”的表述。
4. C 使用 LP/MILP 求解器；先在单日和 2–3 天小样本上核对费用，再跑全年，并做四种预报更新频率的对照。
5. 图表应围绕证据链：A 的温度/浓度剖面和收敛图，B 的误差扇形/搜索轨迹/清除统计，C 的典型日功率平衡、SOC、费用分解和策略对照；本阶段不生成正式论文图。

## 7. 待确认事项与阶段结论

- A：1800 s 后恒温阶段的烘房边界条件、附件 1 的水分浓度语义、收缩与浓度是否双向耦合。
- B：模拟器可执行文件/启动方式/本机端口，以及正式测试开始前的登录、服务器时间校验和日志导出流程。
- C：附件 3 整点预报到 10 分钟控制时段的官方解释，调整购电费用的逐项结算口径，`format2026.doc` 的论文硬约束。

阶段结论：三题的题面、数据和模板已经形成可执行的分析基础；A/B/C 分别对应机理 PDE、黑箱在线搜索、全年储能优化。当前尚未替队伍确定最终选题，也没有生成任何 result 文件或正式测试日志。

## 8. 作者自检与门禁状态

- [x] 已读取三题题面及压缩包目录。
- [x] 已读取 A/C 数据表和模板；B 协议附件已读取关键接口、约束和示例。
- [x] 已记录主要输入结构、时间范围、单位和缺失情况。
- [x] 每个子问题给出不超过两个独立模型体系/模型族的候选路线。
- [x] 已明确关键风险、验证方式和失败条件。
- [ ] 双引擎论文检索：脚本已运行，但两个引擎均 HTTP 429，未获得可引用结果。
- [ ] M1 独立质检：当前环境没有可按技能协议派发且不参与编写的质检 Subagent 工具，因此不能标记 PASS；本交付属于“受限完成”，不是完整门禁通过。

